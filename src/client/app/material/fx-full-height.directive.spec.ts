import { FxFullHeightDirective } from './fx-full-height.directive';

describe('FxFullHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new FxFullHeightDirective();
    expect(directive).toBeTruthy();
  });
});

import { FxFullWidthDirective } from './fx-full-width.directive';

describe('FxFullWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new FxFullWidthDirective();
    expect(directive).toBeTruthy();
  });
});
